let altura = Number(prompt("Qual sua altura?"))
let genero = Number(prompt("Qual seu genero? Digite 1 para masculino, ou, digite 2 para o genero feminino"))
let pesoideal

if (genero = 1){
    (pesoideal = 72.7 * altura - 58 )
    aletr ("Seu peso ideal seria:" + pesoideal)
}else if (genero = 2){
    (pesoideal = 62.1 * altura - 44.7)
    aletr ("Seu peso ideal seria:" + pesoideal)
}else (genero >= 3 && genero <= 0)
    alert("Genero invalido, digite novamente")
